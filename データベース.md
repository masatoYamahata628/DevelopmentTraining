## データベース
#### リレーショナルデータベース
* データを表(テーブル)の概念に従って管理する
  * どのようなデータを扱うかあらかじめ決めておく必要がある
* データベース内に複数のテーブルが存在し必要に応じて連携(リレーション)させることができる
#### データベース管理システム
* データベースの運用/管理に使用するシステム
  * Database Management System(DBMS)
    * リレーションデータベース管理システム(RDBMS)
      * MySQL
      * PostgreSQL
      * IBM DB2
      * Microsoft SQL Server
    * 機能
      * データベース操作
        * SQLという言語を用いる
          * データベースの作成/削除
          * テーブルの作成/更新/削除
          * データの追加/更新/削除
          * データの参照
            * 参照の際にできる演算
              * 選択演算
                * 表から指定した条件を満たす行を取り出す操作
              * 射影演算
                * 表から指定した列を取り出す操作
              * 結合演算
                * 複数の表をそれぞれの表が持つ共通の列で結び付けて取り出す
                  * 例⇒商品ID
              * 和集合演算
              * 差集合演算
              * 積集合演算
              * 直積集合演算
          * ユーザ権限の設定
      * 障害回復
        * 障害の例
          * ハードウェア障害
          * ネットワーク障害
          * ソフトウェア障害
          * 誤操作によるデータ損失
        * バックアップ
          * コピーを作成
          * リストア⇒バックアップデータの再投入
        * ログファイル
          * ジャーナルファイル
          * 操作履歴を記録したファイル
      * トランザクション/排他制御
        * トランザクション
          * 複数の処理が集まって一つの機能を実現するような処理
            * コミット⇒一つの機能に含まれるすべての処理が完了してから全体の変更を確定する
              * エラーが発生した場合は処理全体をキャンセル⇒ロールバック
        * 排他制御
          * 複数のユーザーが同時に利用しても矛盾が生じないようにする仕組み
          * ロック
      * セキュリティ
        * パスワードの設定
        * アクセス権の設定

## 原本項目
* 通番⇒回送当日の付番
* 管理番号⇒ニチレンの管理番号
* クラス
* 車種
* 陸自
* 分類
* 車番
* 地区⇒引取先営業所の地区
* 引取先
* 到着予定⇒営業所帰着予定時間
* 地区⇒納車先営業所の地区
* 納車先
* 地区⇒依頼元営業所の地区
* 依頼元
* 受付番号⇒アルカード管理の管理番号
* 金額(税抜)⇒アルカード入力
* 距離
* 事業部
* 備考⇒キャンセル・車両変更・引取先/納車先変更など

###### データをどう扱うのか？
* データの並び替えや抽出操作をどうするのか？
  * エクセルで送られてくる文字列のデータ
    * 確実に操作ができるようにデータを洗うのか？
      * どう洗うのか？
    * 項目にIDを振ってそのIDで操作するのか？
      * IDを振るのはどうするのか？
        * 文字列検索？確実に正しいIDと紐づくのか？


* そもそも今まで習った知識を使い身につけるための開発だから細部にこだわらなくてよいのか？

* アップロード
* 一行分のデータを取る
* 営業所名を取る
* 番号に変換
* 金額計算