## なぜこのように七面倒くさいコードを書くのか？
```java
InputStream is = new FileInputStream("data.txt"); // ファイルから「バイト」を読む
InputStreamReader isr = new InputStreamReader(is); // バイト⇒文字へ変換
BufferedReader br = new BufferedReader(isr); // 行単位処理バッファリングとメソッド提供
String text = br.readLine(); System.out.println("テキスト：" + text); br.close()
```
* **役割**が違うものを段階的に組み合わせているから
* なぜ役割が違い機能を組み合わせているのか？(一行で一つの指示でできないのか？)
  * **単一責任の原則**で**責務**を分離している
  * 無駄ではなくJavaの**設計思想**そのものの現れ
    * 機能を積み重ねる⇒必要な機能だけ組み合わせ不要な機能は排除できる
###### InputStream
役割⇒ファイルから「バイト(バイナリ)」を読む
ファイルには文字という概念はない
意図⇒バイナリを読む
###### InputStreamReader
役割⇒バイトから文字へ変換(文字コード処理)UTF-8/Shift_JIS
意図⇒文字コード指定
###### BufferedReader
役割⇒バッファリングとメソッドの提供readLine()
意図⇒行単位処理

## コード意図の理解
* 良いコードを書く・設計を理解するための本質
  * **コードは意図を伝える文章**
  * 動くより「**伝わる**」が上位概念
  * 型・API・構造は意図表現の**道具**
  * 「何をしたいか」を**先に言語化**してから書く
* 書くときに意識すべき三つの問い
  * 何をしたいコードか？
    * 読む？
    * 変換？
    * 検証？
    * 集計？
  * その意図は型・API名から伝わるか？
    * **型は意図を語る**
  * 実装詳細を読み手に押し付けていないか？
    * 詳細を知っていないと読めない
    * 高レベルAPIで包む価値がある
* 読むときに意識すべき視点
  * 型
  * 変数名
  * API名
  * 処理の粒度
  * 実装詳細
* 設計できる人の思考順
  1. 意図(目的)
  2. 責務(役割)
  3. 抽象(interface/型)
  4. 意図を上に出し実装は下に隠す
      * 上位は意味、下位は仕組み
  5. 実装(具象クラス)

## レイヤー構造
* レイヤーとは
  * 役割・関心・責務ごとに世界を分け、上位は「何をしたいか」、下位は「どう実現するか」を受け持つ考え方
  * レイヤーとは人間の意味が、物理の現実に壊されないための防波堤
* レイヤーの大原則
  * 上位(意味)は下位(仕組み)を知らない
    * 業務ロジックにbyteは出ない
  * 下位は上位に関心を持たない
    * I/Oが業務ルールを知らない
  * 境界で変換する
    * バイト⇒文字(物理⇒可読)
    * DTO⇒ドメイン(運搬⇒意味)
    * 外部形式⇒内部形式(他人の都合⇒自分の設計)
  * 意味は上に集約する
* 下位レイヤー
  * どう実現するか
* 上位レイヤー
  * 何がしたいか
## ファイルの正体
* コンピュータ⇒ただのバイト列(バイトは意味をもたない)
* 人間⇒文章(文字は解釈結果であり実体ではない)
###### ハードウェア層
* 物理的現実
  * ビットbit(最小単位)
    * 電圧の有無
    * フラグ
    * 命令のON/OFF
###### OS・ランタイム層(OS/CPU)
* 資源管理
  * メモリ
  * プロセス
  * スレッド
  * ファイル(バイト)
    * ファイルや通信など外部とのやり取りはすべてバイト単位で扱われる
###### 言語・ランタイム層(JVM)
###### 標準ライブラリ
###### アプリケーション技術層
###### ドメイン(業務)層(意味)
* 業務・概念の中心(正しさを守る層)
  * ルール
  * 制約
  * 不変条件
###### ユースケース層(振る舞い)
* やりたいことの実行(アプリの目的)
  * 登録する
  * 集計する
  * 処理する

## APIとは
* あるレイヤーが別のレイヤーに対して使ってよい操作だけを公開した約束(窓口)
  * 使い方の説明書
    * 意図・責務
  * 操作できる範囲の宣言
  * 境界のルール
  * 中身を知らなくてよいための契約(中身は知らなくてよい)
* 別レイヤー
  * 別のプログラム
  * 別のレイヤー
  * 別のモジュール
  * 別のクラス
  * 未来の自分
* APIとは境界を越えて使われるすべての「触れてよい部分」

###### コンピュータを扱うとは
* バイト(最終的にはビット)を意味付けして操作すること
* バイトに「意味」を与え制御することが本質
* プログラミング⇒バイトの意味を設計し操作できるようにする
  * 物理・最下層
    * 電圧の有無
    * ビット(0/1)
  * 機械レベル(OS・CPU)
    * バイト
    * 命令コード
    * メモリアドレス
  * プログラミング言語レベル
    * すべてバイト配置の解釈
  * 人間の意図レベル
    * 売り上げを集計する
    * ファイルを読み込む
    * 画面に表示する
* Java
  * 文字列
    * 実体はUTF-16のchar[]
    * ヒープ領域上のバイト列
      * 間接的にバイトを扱っている